[project]
name = "release-tools"
version = "1.4.0"
description = "Internal helpers for cutting and publishing releases."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/release_tools"]
