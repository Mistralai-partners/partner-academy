# A2: Second Input (starter): a different, unseen set of notes

Use this for the second invocation. Testing on a second, unseen input is how you confirm the Skill encodes the contract rather than fitting the first example.

---

design review 8/8: present: sam, lena, me. sam demoed the dashboard redesign, the team liked the new layout but the load time on the reports tab is too high, needs profiling. lena raised that the color contrast fails accessibility on two chart types, must fix before launch. we decided to cut the export-to-PDF feature from v1 to hit the date, it moves to v1.1. open question: should we run a small usability test with 5 users before launch or rely on internal review? actions: sam: profile the reports tab load time by tue. lena: fix the two contrast issues this week. me: decide on the usability test and let the team know by mon. launch target still end of month.

---

> Invoke the same Skill on this input with no extra prompting. The output must still match `output-contract-card.md`.
