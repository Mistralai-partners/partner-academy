[project]
name = "vibe-demo-app"
version = "0.1.0"
description = "A tiny, safe practice project for the Mistral Vibe course."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
